name: xcli
description: >-
  X.com CLI -- the gh equivalent for X.com.
  Same mental model as web/mobile, expressed as CLI subcommands.
version: 0.1.0
repository: https://github.com/cafe01/xcli

publish_to: none

environment:
  sdk: ^3.8.0
resolution: workspace

dependencies:
  args: ^2.6.0
  crypto: ^3.0.6
  http: ^1.3.0
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.25.6

executables:
  x: x
